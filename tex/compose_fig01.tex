% tex/compose_fig01.tex
%
% Figure 1 composite — single page, double-column manuscript width.
%
% Layout:
%   ┌──────────────────────────────────────────┐
%   │  A  Panel A — architecture (full width)  │
%   ├──────────────────┬───────────────────────┤
%   │  B  panelB.pdf   │  C  panelC.pdf        │
%   ├──────────────────┼───────────────────────┤
%   │  D  panelD.pdf   │  E  panelE.pdf        │
%   ├──────────────────┴───────────────────────┤
%   │  F  panelF.pdf   (single column, hsv)    │
%   └──────────────────────────────────────────┘
%
% build.R copies this file into the figure's out/ directory and runs
% xelatex with that as CWD, so each panel is referenced by its bare
% basename.

\documentclass[border = 4mm]{standalone}

\usepackage{compose_helpers}
\usepackage{graphicx}
\usepackage{tikz}

\begin{document}

\begin{tikzpicture}[remember picture]

    % ---- Panel A — full width ------------------------------------------
    \node[anchor = north west, inner sep = 0pt]
        (panela) at (0, 0)
        {\includegraphics[width = 180mm]{panelA.pdf}};
    \node[anchor = north west,
          font = \sffamily\bfseries,
          inner sep = 2pt]
        at ($(panela.north west) + (-2mm, 2mm)$)
        {A};

    % ---- Panels B + C (row 2) ------------------------------------------
    \node[anchor = north west, inner sep = 0pt,
          below = 4mm of panela.south west]
        (panelb)
        {\includegraphics[width = 87mm]{panelB.pdf}};
    \node[anchor = north west, inner sep = 0pt,
          right = 4mm of panelb]
        (panelc)
        {\includegraphics[width = 87mm]{panelC.pdf}};
    \node[anchor = north west, font = \sffamily\bfseries, inner sep = 2pt]
        at ($(panelb.north west) + (-2mm, 2mm)$) {B};
    \node[anchor = north west, font = \sffamily\bfseries, inner sep = 2pt]
        at ($(panelc.north west) + (-2mm, 2mm)$) {C};

    % ---- Panels D + E (row 3) ------------------------------------------
    \node[anchor = north west, inner sep = 0pt,
          below = 4mm of panelb.south west]
        (paneld)
        {\includegraphics[width = 87mm]{panelD.pdf}};
    \node[anchor = north west, inner sep = 0pt,
          right = 4mm of paneld]
        (panele)
        {\includegraphics[width = 87mm]{panelE.pdf}};
    \node[anchor = north west, font = \sffamily\bfseries, inner sep = 2pt]
        at ($(paneld.north west) + (-2mm, 2mm)$) {D};
    \node[anchor = north west, font = \sffamily\bfseries, inner sep = 2pt]
        at ($(panele.north west) + (-2mm, 2mm)$) {E};

    % ---- Panel F (row 4, single column) --------------------------------
    \node[anchor = north west, inner sep = 0pt,
          below = 4mm of paneld.south west]
        (panelf)
        {\includegraphics[width = 87mm]{panelF.pdf}};
    \node[anchor = north west, font = \sffamily\bfseries, inner sep = 2pt]
        at ($(panelf.north west) + (-2mm, 2mm)$) {F};

\end{tikzpicture}

\end{document}
