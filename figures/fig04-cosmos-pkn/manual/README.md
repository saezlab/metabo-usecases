# fig04-cosmos-pkn/manual — schematic placeholders

Mode: **manual** (all three schematics are hand-authored; not regenerated by
the pipeline).

## Expected assets

| File | Purpose |
|------|---------|
| `pkn-to-binary-network.pdf` / `.svg` | Cartoon showing how the OmniPath Metabo PKN collapses to a signed binary network for COSMOS. |
| `regulation-types.pdf` / `.svg` | Legend-style schematic listing the six edge categories (activating, inhibitory, expression, …) with their sign conventions. |
| `moon-activity-inference.pdf` / `.svg` | Simplified MOON activity-inference diagram: prior-knowledge edges → observed metabolomics + transcriptomics constraints → inferred node activities. |

## Capture notes

All three schematics must be authored in Inkscape (or equivalent vector tool)
and exported as both PDF and SVG.  Keep source `.svg` under version control
here; the `.pdf` variants are derived at export time.

When an asset is ready, replace its `.gitkeep` placeholder and update this
README with:
- `last_edit_date: YYYY-MM-DD`
- `author: <name>`
- `tool_version: <inkscape version>`
- `sha256: <hex>`

## Placeholder status

| Asset | Status |
|-------|--------|
| pkn-to-binary-network | PLACEHOLDER — awaiting hand-authored schematic |
| regulation-types | PLACEHOLDER — awaiting hand-authored schematic |
| moon-activity-inference | PLACEHOLDER — awaiting hand-authored schematic |
