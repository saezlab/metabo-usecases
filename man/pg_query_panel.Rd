% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-deployment.R
\name{pg_query_panel}
\alias{pg_query_panel}
\title{Run a query against the deployment a (panel, facet) routes to}
\usage{
pg_query_panel(panel_id, sql, ..., facet = NULL)
}
\arguments{
\item{panel_id}{Character: panel identifier (see
\code{\link{panel_deployment}}).}

\item{sql}{Character: SQL text.}

\item{...}{Parameters bound via \code{DBI::dbGetQuery(params = ...)}.}

\item{facet}{Character or \code{NULL}: facet identifier.}
}
\value{
A tibble of query results carrying \code{"sql"},
\code{"result_hash"}, and \code{"deployment"} attributes.
}
\description{
Thin shim around \code{\link{pg_query}} that resolves the
deployment first, opens / re-uses a handle via
\code{\link{pg_connect_panel}}, runs the query, and tags the
returned tibble's attribute \code{"deployment"} with the
resolved label so the provenance sidecar can collect every
deployment the artifact touched (FR-031, sidecar
\code{deployments: array}).
}
\examples{
\dontrun{
rows <- pg_query_panel(
    "fig01-architecture",
    "SELECT COUNT(*) FROM entity",
    facet = "structures"      # routes to dev5 (post-2026-06-14)
)
attr(rows, "deployment")
}

}
