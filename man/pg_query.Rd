% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-postgres.R
\name{pg_query}
\alias{pg_query}
\title{Run a query, return a tibble, and record the result for provenance}
\usage{
pg_query(con, sql, ...)
}
\arguments{
\item{con}{A DBI Postgres connection from \code{\link{pg_connect}}.}

\item{sql}{Character: parameterized SQL.}

\item{...}{Parameters bound via \code{DBI::dbGetQuery(params = ...)}.}
}
\value{
A tibble of query results with \code{sql} and
\code{result_hash} attributes.
}
\description{
Wraps \code{DBI::dbGetQuery} so every query the pipeline executes is
logged and fingerprinted. The returned tibble carries two
attributes: \code{"sql"} (the query text) and \code{"result_hash"}
(SHA-256 truncated to 12 hex chars over the canonical result
representation). The provenance sidecar writer picks these up.
}
\examples{
\dontrun{
con <- pg_connect()
rows <- pg_query(con, "SELECT COUNT(*) FROM data_source")
attr(rows, "result_hash")
}

}
