% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-resource_counts.R
\name{section_contributing_resources}
\alias{section_contributing_resources}
\title{Per-section contributing-resource list (FR-043f)}
\usage{
section_contributing_resources(section_id, panel_id = "fig01-architecture")
}
\arguments{
\item{section_id}{Integer (1..5).}

\item{panel_id}{Character: panel id used for deployment routing.
Defaults to \code{"fig01-architecture"}.}
}
\value{
A tibble with columns \code{resource_name} (character) and
\code{n_rows} (integer). The tibble also carries
\code{attr(., "sql")} and \code{attr(., "result_hash")} +
\code{attr(., "deployment")} from \code{pg_query_panel}.
}
\description{
Returns the distinct \code{data_source.name} values contributing= 1 row to the named section's underlying join, plus the rowcount per source. The full list is written into\code{stats.json -> sections[i].resources} (not just the count) sothe author can cross-check which resources back each headlinenumber on the Panel A diagram.
}
\details{
Runs via \code{\link{pg_query_panel}} so the deployment routing
goes through the FR-030 matrix.
}
